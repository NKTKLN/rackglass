# Rust build output
/target

# Container build output (tools/arm64/build.sh)
/build/

# Editors
*.swp
.idea/
.vscode/

# Runtime configuration: a real endpoint and topology, not for the repo.
# Copy config.env.example to config.env and edit that.
config.env
config.*.env
