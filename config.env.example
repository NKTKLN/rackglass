# Build-time configuration for rackglass.
#
#   cp config.env.example config.env
#   flutter run -d linux --dart-define-from-file=config.env
#
# These are compile-time defines, not runtime environment variables: the values
# are baked into the binary by the build that reads this file. config.env is
# gitignored, so your own endpoint and topology stay out of the repository.
#
# Anything left out here falls back to the default in lib/src/config.dart.

# Base URL of the Prometheus server, no trailing slash.
PROM_URL=http://localhost:9090

# Interfaces kept out of the aggregate network totals, as a PromQL regex.
# The default excludes loopback and the usual Proxmox/Docker/libvirt virtual
# devices, so a forwarded packet is not counted once on the physical NIC, again
# on the bridge and again on the tap device.
NET_DEVICE_EXCLUDE=^(lo|veth.*|tap.*|fwbr.*|fwln.*|fwpr.*|vmbr.*|docker.*|br-.*|virbr.*)$

# Capture geometry requested from the V4L2 device. The card scales the source
# into whatever mode it is asked for; see the README for the measured rates.
CAPTURE_W=1280
CAPTURE_H=720
CAPTURE_FPS=30
